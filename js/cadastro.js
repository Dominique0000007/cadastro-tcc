document.querySelector('.cadastro').addEventListener('click', function(event) {
    event.preventDefault(); // Impede o comportamento padrão (se for um link ou botão)
    window.location.href = 'cadastro.html'; // Redireciona para 'cadastro.html'
});
